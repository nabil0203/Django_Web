# 🖇️Aggregate

- Aggregate used for **a summary** of the statistics across the entire Project/QuerySet
- Details --> [Click Here](https://www.geeksforgeeks.org/python/aggregate-vs-annotate-in-django/)
- Inside Project ---> [Click Here](https://github.com/nabil0203/ShopNest/blob/main/shop/views.py#L107)

---


### Import:
```py
from django.db.models import Avg, Sum, Max, Min, Count
```
### Code:
```py
    # calculating the minimum value of the 'price' field from the all 'products' and storing it in the 'min_price' variable.
    min_price = products.aggregate(Min('price'))['price__min']
    
    
    # calculating the maximum value of the 'price' field from the all 'products' and storing it in the 'max_price' variable.
    max_price = products.aggregate(Max('price'))['price__max']
```