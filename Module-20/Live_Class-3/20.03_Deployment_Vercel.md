# Did not work


# 1. Run in the main project which has app in it
```bash
python -m pip freeze > requirements.txt
```
- a `requirements.txt` file will be created
- 
- 


# 2. create `versal.json`
```json
{
    "builds": [{
        "src": "taskify/wsgi.py",                       // taskify = project name
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    },
    {
        "src": "build_files.sh",
        "use": "@vercel/static-build",
        "config": { "distDir": "staticfiles_build" }
    }],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/(.*)",
            "dest": "taskify/wsgi.py"                       // taskify = project name
        }
    ]
}
```
- 
- 
- 


# 3. create `build_start.sh`
```sh
echo "BUILD START"
python3.9 -m pip install -r requirements.txt
python3.9 manage.py collectstatic --noinput --clear
echo "BUILD END"
```
- 
- 
- 


# 4. `wsgi.py`
```
app = application
```
- add this line
- 
- 


# 5. `settings.py`
- Import
```py
import os
```

- add these 3 lines under `STATIC_URL = 'static/'`
```py
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles_build', 'static')
MEDIA_URLS ='/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```
- 
- 


# 6. `settings.py`
- Comment database
```py
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.sqlite3',
#         'NAME': BASE_DIR / 'db.sqlite3',
#     }
# }
```
- 
- 


# 7. `settings.py`
- make Allowed host `'*'`
```py
ALLOWED_HOSTS = ['*']
```
- 
- 


# 8. Vercel Website
- make sure that Vercel is importing the github branch that has the `vercel.json` file
- otherwise it will not work
- 

