# 🔍Odering

## ▶️Setup

### 📌Follow `24.06_Filtering.md`


------------------------


### 4️⃣`views.py` [Extra Ordering part]

```py
class AuthorViewSet(viewsets.ModelViewSet):

    queryset = Author.objects.all()
    serializer_class = AuthorSerializer



     # DRF filtering

    filter_backends = [filters.SearchFilter, filters.OrderingFilter]            # ordering based on all the fields of object



    # DRF ordering

    ordering_fields = ['name']                                                  # ordering based on just name
```




---------------




- Change Can be made
```py
ordering_fields = ['id']                                                  # ordering based on just id
```

