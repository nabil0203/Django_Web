# 🔁 Main Difference with the Previous Knowledge

## 📌**Previously:**  
- When a URL was hit, the server rendered an **HTML template** 

- The server was responsible for
    - Rendering the UI
    - Deciding how data would be displayed
- Example:
```py
return render(request, "users.html", {"users": users})  
```


-----------------------
-----------------------


## 📌**Now:** 
- When a URL is hit, the server returns a **JSON response** 
- Only provides **data in JSON format**
- Data only, no UI.

- Example:
```py
return JsonResponse({"users": users})
```
- The frontend *(React/Vue)* is responsible for:
    - UI rendering
    - Presentation
    - User interaction


