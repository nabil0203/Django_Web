# 📏Permission Policy

### 🔗Documentation ---> [`Click Here`](https://www.django-rest-framework.org/api-guide/permissions/)
- Django REST framework -> API Guide -> Permissions -> Setting the permission policy



------------------------

## 🌐Globally permission policy
- The default permission policy may be set globally, using the DEFAULT_PERMISSION_CLASSES setting
- For example: `settings.py`
```py
REST_FRAMEWORK = {
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ]
}
```

### 🚨By doing this, The whole system can't be used by any unauthenticated user

